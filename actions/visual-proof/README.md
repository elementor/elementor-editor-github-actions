# Visual proof

Shared author + Playground recorder for `elementor/elementor` and
`elementor/elementor-pro`.

Callers keep their own `playground-preview` workflow. This folder is the
scripts; the composite entrypoints are:

- `actions/visual-proof-author`
- `actions/visual-proof-shots`

See those READMEs for wrapper workflow YAML.
