{
	"name": "@elementor/post-release-verify",
	"author": "Elementor Team",
	"license": "GPL-3.0-or-later",
	"private": true,
	"scripts": {
		"build": "tsup --config ./tsup.config.ts",
		"dev": "npm run build -- --watch",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"@actions/core": "^1.11.1",
		"@elementor/editor-github-actions-utils": "1.0.0"
	},
	"devDependencies": {
		"tsup": "^8.5.0",
		"vitest": "^4.1.9"
	}
}
