# elementor-editor-github-actions

Editor-focused GitHub Actions live in `actions/`.

## Kubernetes ephemeral environments (proposal)

See [`ephemeral-operator/`](ephemeral-operator/) for a Kubernetes-native
replacement of the GitHub Actions–orchestrated ephemeral environments platform.
This tree is intended to be extracted into its own repository after review.
